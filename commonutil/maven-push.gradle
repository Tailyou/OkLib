apply plugin: 'maven'

uploadArchives {
    repositories.mavenDeployer {
        pom.groupId = 'com.tailyou.library'
        pom.artifactId = 'commonutil'
        pom.version = '0.0.1'
        pom.packaging = "aar"
        repository(url: "https://github.com/Tailyou/OkLib/master")
    }
}