<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/setting_primary"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/ivBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:paddingBottom="@dimen/action_iv_padding_vertical"
        android:paddingLeft="@dimen/action_iv_padding_left"
        android:paddingRight="@dimen/action_iv_padding_right"
        android:paddingTop="@dimen/action_iv_padding_vertical"
        android:src="@mipmap/ic_back" />

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="12dp"
        android:layout_marginTop="12dp"
        android:textColor="@color/setting_accent"
        android:textSize="@dimen/action_text_size" />

</RelativeLayout>